# ─── Core ML ─────────────────────────────────────────────────────────────────
# CPU-only PyTorch (меньше размер, достаточно для экспорта)
# Для GPU замените index-url: https://download.pytorch.org/whl/cu121
torch==2.10.0
torchvision==0.25.0

# ─── ONNX ────────────────────────────────────────────────────────────────────
onnx==1.20.1
onnxruntime==1.24.1          # для верификации inference
# onnxruntime-training       # для генерации training artifacts
# Установка: pip install onnxruntime-training
# (требует отдельного индекса или сборки из исходников)

# ─── Утилиты ─────────────────────────────────────────────────────────────────
# onnxsim                    # упрощение/оптимизация графа (требует cmake)
# Установка: pip install onnxsim
numpy==2.3.5
Pillow==11.2.1               # для тестового прогона
